#github Pull request

github.com - Pull request - 1. 수업소개 
https://www.youtube.com/watch?v=uvsz2XgRPfM

github.com - Pull request - 2. 실습환경
https://www.youtube.com/watch?v=kkQiIXH5w2A

github.com - Pull request - 3. pull request 만들기
https://www.youtube.com/watch?v=Ru9qv-tHj7I


#CSS 기본정리

1. CSS기본개념
2. CSS selector(Rule Set, Style Sheet)
3. HTML과 CSS 연동방법 -> <head> 안에 CSS파일로 링크하기
4. Reset CSS(normalize.css)


#CSS 크기단위

1. 픽셀,퍼센트,em,rem
2. 반응형 Viewport단위(vw,vh,vmin,vmax)
3. 색상 표현 단위(컬러명,HEX코드,RGB,RGBA)
4. opacity 프로퍼티

#CSS Selector

1. 전체 셀렉터
2. 태그 셀렉터
3. ID 셀렉터
4. Class 셀렉터
5. attribute 셀렉터

#복합 CSS Selector

1. 후손 셀렉터
2. 자식 셀렉터
3. 인접 형제 셀렉터
4. 일반 형제 셀렉터

#가상 클래스 셀렉터 -> 가상 요소 셀렉터 이외에는 한 개의 콜론(:)을 사용

1. 가상 클래스 종류(link,visited,hover,active,focus)
2. UI 요소 상태 셀렉터(enavled.disabled,checked,indeterminate)
3. 구조 가상 클래스 셀렉터(first-chind,last-child,nth-child(n),nth-last-child(n))
4. 부정 셀렉터
5. 정합성 체크 셀렉터(valid/invalid-> required,pattern)
6. 가상 요소 셀렉터 -> 콜론 두개 사용(::) / (first-letter,first-line,after,before,selection)